<!-- Table of Contents -->
<% if (page.catalog || true) { %>
    <% if (is_post()){ %>
      <!-- <aside id="sidebar">
        <div id="toc" class="toc-article">
        <strong class="toc-title"><%= __('contents') %></strong>
        <% if (toc(page.content) != ""){ %>
          <%- toc(page.content, { "class": "toc-nav" , "min_depth"}) %>
        <% } else { %>
          <ol class="nav"><%= __('none') %></ol>
        <% } %>
        </div>
      </aside> -->
      <% if (!index && theme.toc){ %>
        <script>
          var tocEx = function(el){
            var toc = document.querySelector(el), content = toc.innerHTML;
            content = content.replace('<!-- ttoc -->', '<div class="toc">').replace('<!-- ttocstop -->', '</div>');
            toc.innerHTML = content;
          }('.article-entry');
        </script>
        <% } %>
    <% } %>
<% } %>
